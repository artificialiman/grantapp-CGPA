<!--
	This page never actually renders — +page.server.ts's load always
	redirects (to /signup or /dashboard) before this component would
	be shown. It exists only because SvelteKit requires a +page.svelte
	to pair with +page.server.ts's load function.
-->
