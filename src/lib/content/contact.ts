/**
/**
 * Shared contact link. Per cgpa-state-of-play.md Section 3: "All CGPA
 * WhatsApp-routing triggers... route to the SAME SINGLE WhatsApp
 * group [as UTME] — no per-trigger group split." Not a separate
 * CGPA-specific group.
 *
 * Real group invite link, received directly and confirmed correct —
 * resolves the placeholder both this file's earlier version and
 * grantapp-shell's own contact.ts had been carrying. grantapp-shell's
 * copy was updated in parallel to match; the two are meant to point
 * at the identical destination.
 */
export const WHATSAPP_URL = 'https://chat.whatsapp.com/EGSE3SrFC0w3OuVZ7zJXeU';
